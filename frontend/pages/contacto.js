import Auth from "@FMaidana07/components/auth"
import Head from "next/head";
const contact = () => {
  return (
    <Auth>
      <Head>
        <title>Contacto - [Maidana-Project]</title>
        <meta name="viewport" content="width=device-width, initial-scale=1" />
      </Head>

      <div>En proceso...</div>
    </Auth>

  )
}

export default contact